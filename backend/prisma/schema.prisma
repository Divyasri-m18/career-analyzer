generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./career.db"
}

model Analysis {
  id        Int      @id @default(autoincrement())
  name      String?
  skills    String
  domains   String
  score     Int
  createdAt DateTime @default(now())
}
